{
  "buildCommand": "pnpm --filter @workspace/luxestate run build",
  "outputDirectory": "artifacts/luxestate/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
